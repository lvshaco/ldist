---
# vars
- name: Fix server port by portoffset
  set_fact: 
    gateport: "{{gateport+portoffset}}"
    cmdport: "{{cmdport+portoffset}}"
    redis_port: "{{redis_port+portoffset}}"
  tags: always

- debug: msg="gateport={{gateport}} cmdport={{cmdport}} redis_port={{redis_port}}"
  tags: always
